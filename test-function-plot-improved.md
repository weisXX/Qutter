# 函数绘制测试

## 测试 y = 2x^2

```function
2*x^2
```

或者使用更详细的配置：

```function
{
  "expression": "2*x^2",
  "xMin": -5,
  "xMax": 5,
  "yMin": -10,
  "yMax": 50,
  "title": "y = 2x² 函数图像",
  "color": "#e74c3c"
}
```

## 其他测试函数

```function
sin(x)
```

```function
{
  "expression": "x^3 - 2*x + 1",
  "xMin": -3,
  "xMax": 3,
  "yMin": -10,
  "yMax": 10
}
```